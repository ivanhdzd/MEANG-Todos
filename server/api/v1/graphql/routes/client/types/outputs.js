import { TodoType } from '../../../types/outputs/todo';
import { UserType } from '../../../types/outputs/user';

/** Output type definitions */
export const OutputDef = [
	TodoType,
	UserType
].join('\n');