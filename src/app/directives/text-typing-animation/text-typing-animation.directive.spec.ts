import { TextTypingAnimationDirective } from './text-typing-animation.directive';

describe('TextTypingAnimationDirective', () => {
	it('should create an instance', () => {
		const directive = new TextTypingAnimationDirective();
		expect(directive).toBeTruthy();
	});
});